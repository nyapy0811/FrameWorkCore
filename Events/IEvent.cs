namespace Framework.Core
{
    /// <summary>
    /// 모든 이벤트가 구현하는 마커 인터페이스.
    /// 이벤트는 "무슨 일이 일어났는가"를 담는 데이터 묶음일 뿐이다.
    /// 보통 struct로 만들어 가볍게 쓴다.
    /// </summary>
    public interface IEvent { }
}
